apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-config.fullname" . }}
  labels:
    {{- include "aws-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-config.selectorLabels" . | nindent 4 }}
